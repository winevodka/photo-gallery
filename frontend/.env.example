NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Admin dashboard (/admin) — server-side only, never exposed to the browser
# Password used to log into /admin
ADMIN_PASSWORD=change-me-to-a-random-secret
# Must match the backend's ADMIN_API_KEY exactly
ADMIN_API_KEY=change-me-to-a-random-secret
